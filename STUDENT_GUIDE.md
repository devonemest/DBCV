# Руководство для студентов: Реализация интеграций в DBCV

## ⚠️ Важное предупреждение (Дисклеймер)

**Мы начинаем трек выполнения практического задания.**

Вам будет дана возможность использовать **вайбкодинг** (AI-ассистированное программирование) и **ИИ-инструменты** для участия в разработке сложной платформы. Конкретно вы получите задания по реализации интеграций.

**Что это значит простыми словами:**

- 🎯 **Вы будете работать с AI** - это нормально и даже приветствуется! Мы учим вас использовать современные инструменты.
- 🤖 **AI будет писать код за вас** - вы будете давать ему задания (промпты), а он будет генерировать код.
- 📚 **Вы будете учиться** - даже если AI пишет код, вы должны понимать, что он делает и как это работает.
- ✅ **Ваша задача** - правильно формулировать запросы к AI, проверять код, тестировать и исправлять ошибки.

**Это не значит, что вы "обманываете"** - это современный подход к разработке! Даже профессиональные программисты используют AI-ассистентов.

**Если вы не понимаете что-то** - это нормально! Читайте документацию, задавайте вопросы, изучайте примеры. Мы все здесь, чтобы помочь вам.

---

## Содержание

1. [Шаг 1: Ознакомиться](#шаг-1-ознакомиться)
2. [Шаг 2: Выбрать задачу](#шаг-2-выбрать-задачу)
3. [Шаг 3: Установить необходимые программы](#шаг-3-установить-необходимые-программы)
4. [Шаг 4: Установить backend и frontend](#шаг-4-установить-backend-и-frontend)
5. [Шаг 5: Реализовать задачу и протестировать](#шаг-5-реализовать-задачу-и-протестировать)
6. [Шаг 6: Записать видео](#шаг-6-записать-видео)
7. [Шаг 7: Создать Pull Request](#шаг-7-создать-pull-request)
8. [Дополнительные разделы](#дополнительные-разделы)
9. [Терминология](#терминология)

---

## Шаг 1: Ознакомиться

### Что такое DBCV? (Простыми словами)

**DBCV** - это платформа, где можно создавать ботов (например, для Telegram) **без написания сложного кода**.

Представьте конструктор LEGO, но для ботов:
- Вы берете готовые блоки (интеграции)
- Соединяете их между собой
- Получаете готового бота

**Пример:** Хотите бота, который отправляет сообщения в Telegram? Берете блок "Telegram Send Message", настраиваете его, и готово!

### Что такое интеграции? (Простыми словами)

**Интеграции** - это готовые "блоки" для работы с разными сервисами.

**Примеры:**
- Telegram интеграция - позволяет боту отправлять сообщения в Telegram
- OpenAI интеграция - позволяет боту использовать ChatGPT
- Google Sheets интеграция - позволяет боту читать/писать в таблицы Google

**Ваша задача:** Создать такие "блоки" (интеграции) для разных сервисов.

### Что такое пресеты? (Простыми словами)

**Пресеты** - это готовые "шаблоны" для типовых задач.

**Примеры:**
- IF/ELSE пресет - "если условие выполнено, сделай одно, иначе другое"
- Loop пресет - "повтори действие несколько раз"
- Delay пресет - "подожди 5 секунд перед следующим действием"

**Ваша задача:** Создать такие "шаблоны" (пресеты) для разных задач.

### Ваша задача (Что именно нужно делать?)

**Ваша задача:** Реализовать интеграции и/или пресеты.

**Как это делать:**
1. **Выбрать задачу** из списка (например, "Telegram Send Photo")
2. **Использовать AI** для написания кода (ChatGPT, Claude, Cursor и др.)
3. **Проверить код** - убедиться, что он работает
4. **Протестировать** - проверить, что интеграция работает в боте
5. **Записать видео** - показать, как это работает
6. **Создать Pull Request** - отправить код на проверку

**Важно:** Вы **НЕ пишете код вручную**! Вы используете AI-инструменты, которые будут писать код за вас. Ваша задача - правильно объяснить AI, что нужно сделать.

### Обзор процесса работы

Весь процесс состоит из 7 основных шагов:

1. ✅ **Ознакомиться** - понять что такое DBCV, интеграции и пресеты
2. ✅ **Выбрать задачу** - выбрать интеграцию или пресет из списка
3. ✅ **Установить программы** - установить AI-инструменты, Git, Docker и др.
4. ✅ **Установить проект** - настроить backend и frontend
5. ✅ **Реализовать и протестировать** - создать интеграцию с помощью AI и проверить её работу
6. ✅ **Записать видео** - показать результат работы
7. ✅ **Создать Pull Request** - отправить код на проверку

---

## Шаг 2: Выбрать задачу

### Шаг 2.1: Открыть список задач

1. Открыть файл [TASK_DISTRIBUTION.md](./TASK_DISTRIBUTION.md)
2. Просмотреть доступные интеграции и пресеты
3. Обратить внимание на сложность задач:
   - **Низкая** - рекомендуются для начала
   - **Средняя** - для тех, кто уже выполнил несколько задач
   - **Высокая** - для опытных студентов

### Шаг 2.2: Выбрать задачу

**Рекомендации:**
- Начните с простых задач (сложность "Низкая")
- Выберите интеграцию из знакомого сервиса (например, Telegram)
- Можно взять несколько интеграций из одной категории

**Примеры хороших задач для начала:**
- Telegram Send Message
- Telegram Get Updates
- Telegram Send Photo
- Discord Send Message

### Шаг 2.3: Зарезервировать задачу

1. Создать issue в GitHub/GitLab с названием интеграции/пресета
2. Указать в issue:
   - Название интеграции/пресета
   - Тип задачи (интеграция или пресет)
   - Категорию
   - Оценочный срок выполнения
3. Дождаться подтверждения от менеджера

### Шаг 2.4: Изучить требования

**Для интеграций:**
1. Прочитать документацию по интеграциям: `backend/app/integrations/README.md`
2. Изучить пример: `backend/app/integrations/telegram/send_message.py`
3. Проверить библиотеку в: `backend/app/integrations/SAFE_LIBRARIES.md`
4. Подготовить промпт для нейросети (см. [STUDENT_AI_PROMPT.md](./STUDENT_AI_PROMPT.md))

**Для пресетов:**
1. Прочитать документацию по пресетам: `backend/app/presets/README.md` (если есть)
2. Изучить пример: `backend/app/presets/conditional.py` (IF/ELSE пресет)
3. Изучить базовый класс: `backend/app/presets/base.py`
4. Подготовить промпт для нейросети (адаптировать из [STUDENT_AI_PROMPT.md](./STUDENT_AI_PROMPT.md))

**Чеклист:**
- [ ] Задача выбрана из списка
- [ ] Issue создан и подтвержден
- [ ] Документация изучена
- [ ] Примеры изучены
- [ ] Промпт для AI подготовлен

---

## Шаг 3: Установить необходимые программы

### Шаг 3.1: Установить AI-инструмент ⭐

**Это самый важный шаг!** Без AI-инструмента вы не сможете выполнить задание.

#### Что такое AI-инструменты?

AI-инструменты - это программы, которые помогают писать код. Они понимают ваши запросы на русском языке и генерируют код за вас.

#### Какие инструменты можно использовать?

Вы можете использовать **ЛЮБЫЕ** из следующих инструментов (или их аналоги):

##### 1. Cursor (Рекомендуется) ⭐

**Что это:** Редактор кода с встроенным AI-ассистентом.

**Как установить:**
1. Перейти на сайт: https://cursor.sh
2. Нажать "Download" (Скачать)
3. Установить программу (как обычную программу)
4. Открыть Cursor
5. Зарегистрироваться (можно через GitHub или Google)

**Как использовать:**
- Открыть файл в Cursor
- Нажать `Ctrl+K` (Windows/Linux) или `Cmd+K` (Mac)
- Написать запрос на русском языке (например: "Создай интеграцию для отправки сообщений в Telegram")
- AI сгенерирует код

**Плюсы:**
- ✅ Работает прямо в редакторе кода
- ✅ Понимает контекст вашего проекта
- ✅ Бесплатный план доступен

##### 2. GitHub Copilot

**Что это:** Расширение для VS Code и других редакторов от GitHub.

**Как установить:**
1. Установить VS Code: https://code.visualstudio.com
2. Открыть VS Code
3. Перейти в Extensions (Расширения) - иконка слева
4. Найти "GitHub Copilot"
5. Нажать "Install" (Установить)
6. Войти через GitHub аккаунт

**Как использовать:**
- Начать печатать код
- AI будет предлагать варианты автоматически
- Нажать `Tab` чтобы принять предложение

**Плюсы:**
- ✅ Работает прямо в редакторе кода
- ✅ Понимает контекст вашего проекта
- ✅ Бесплатный план доступен для студентов

##### 3. Codeium

**Что это:** Бесплатная альтернатива Copilot.

**Как установить:**
1. Перейти на сайт: https://codeium.com
2. Нажать "Get Started" (Начать)
3. Выбрать редактор (VS Code, PyCharm и др.)
4. Следовать инструкциям по установке

**Плюсы:**
- ✅ Полностью бесплатный
- ✅ Работает как Copilot
- ✅ Не требует подписки

##### 4. Codex (ChatGPT для VS Code)

**Что это:** Расширение для VS Code от OpenAI, которое интегрирует ChatGPT прямо в редактор.

**Как установить:**
1. Установить VS Code: https://code.visualstudio.com
2. Открыть VS Code
3. Перейти в Extensions (Расширения) - иконка слева
4. Найти "Codex" или "ChatGPT"
5. Нажать "Install" (Установить)
6. Войти через OpenAI аккаунт (или создать новый)

**Как использовать:**
- Открыть файл в VS Code
- Нажать `Ctrl+Shift+P` (Windows/Linux) или `Cmd+Shift+P` (Mac)
- Ввести "Codex" или "ChatGPT"
- Выбрать команду и написать запрос
- AI сгенерирует код прямо в редакторе

**Плюсы:**
- ✅ Работает прямо в VS Code
- ✅ Не нужно переключаться в браузер
- ✅ Понимает контекст файла
- ✅ Можно задавать вопросы и уточнять

##### 5. ChatGPT / Claude (через браузер)

**Что это:** AI-чатботы, которые могут писать код.

**Как использовать:**
1. Открыть сайт: https://chat.openai.com (ChatGPT) или https://claude.ai (Claude)
2. Зарегистрироваться (если еще не зарегистрированы)
3. Написать запрос (например: "Создай интеграцию для Telegram Send Message на Python")
4. Скопировать код из ответа
5. Вставить в файл

**Плюсы:**
- ✅ Работает в браузере
- ✅ Не нужно ничего устанавливать
- ✅ Можно задавать вопросы и уточнять

**Минусы:**
- ⚠️ Нужно копировать код вручную
- ⚠️ Не понимает контекст вашего проекта автоматически

##### 6. Другие варианты

Вы можете использовать **ЛЮБЫЕ** аналогичные инструменты:
- **Tabnine** - AI-ассистент для кода
- **Amazon CodeWhisperer** - от Amazon
- **JetBrains AI Assistant** - для PyCharm
- **Любые другие** AI-инструменты для программирования

#### Какой инструмент выбрать?

**Для начинающих:**
1. **Cursor** - самый простой и удобный
2. **ChatGPT через браузер** - если не хотите ничего устанавливать

**Для продвинутых:**
- **GitHub Copilot** - если у вас есть подписка
- **Codeium** - бесплатная альтернатива

**Можно использовать несколько инструментов одновременно!**

#### Проверка работы AI-инструмента

**Проверьте, что инструмент работает:**
- Откройте любой файл с кодом
- Попробуйте попросить AI написать простой код
- Если AI отвечает - все готово!

### Шаг 3.2: Установить Git

**Что это:** Система контроля версий для работы с кодом.

**Как установить:**
1. Перейти на сайт: https://git-scm.com/downloads
2. Скачать версию для вашей ОС
3. Установить (можно оставить настройки по умолчанию)
4. Проверить установку:
   ```bash
   git --version
   ```

**Настроить Git (если еще не настроен):**
```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"
```

### Шаг 3.3: Установить Docker (рекомендуется)

**Что это:** Платформа для запуска приложений в контейнерах.

**Как установить:**
1. Перейти на сайт: https://www.docker.com/products/docker-desktop/
2. Скачать Docker Desktop для вашей ОС
3. Установить и запустить Docker Desktop
4. Проверить установку:
   ```bash
   docker --version
   docker-compose --version
   ```

**Важно:** Docker Desktop должен быть запущен перед использованием команд `docker-compose`.

### Шаг 3.4: Установить Python (для backend)

**Что это:** Язык программирования для backend.

**Требования:** Python 3.12 или выше

**Как установить:**
1. Перейти на сайт: https://www.python.org/downloads/
2. Скачать последнюю версию Python 3.12+
3. Установить (обязательно отметить "Add Python to PATH")
4. Проверить установку:
   ```bash
   python --version
   ```

### Шаг 3.5: Установить Node.js (для frontend)

**Что это:** Платформа для запуска JavaScript приложений.

**Требования:** Node.js 18 или выше

**Как установить:**
1. Перейти на сайт: https://nodejs.org/
2. Скачать LTS версию (рекомендуется)
3. Установить (npm установится автоматически)
4. Проверить установку:
   ```bash
   node --version
   npm --version
   ```

**Чеклист:**
- [ ] AI-инструмент установлен и работает
- [ ] Git установлен и настроен
- [ ] Docker установлен (если используется)
- [ ] Python 3.12+ установлен
- [ ] Node.js 18+ установлен

---

## Шаг 4: Установить backend и frontend

### Шаг 4.1: Клонировать репозиторий

```bash
git clone <URL_РЕПОЗИТОРИЯ>
cd DBCV
```

### Шаг 4.2: Настроить файл окружения

1. Скопировать файл окружения:
   ```bash
   cp env.example env.dev
   ```

2. Открыть файл `env.dev` в текстовом редакторе

3. Заполнить необходимые данные (если не знаете что - можно оставить значения по умолчанию для начала)

### Шаг 4.3: Установить и запустить Backend

#### Вариант 1: Через Docker (рекомендуется)

**Запуск:**
```bash
docker-compose -f docker-compose.dev.yml --env-file env.dev up -d
```

**Примечание:** Файл `env.dev` автоматически используется через настройку `env_file` в `docker-compose.dev.yml`.

**Проверка статуса:**
```bash
docker-compose -f docker-compose.dev.yml ps
```

**Просмотр логов:**
```bash
docker-compose -f docker-compose.dev.yml logs -f backend
```

**Остановка:**
Сочетание клавиш CTRL+C в терминале или 

```bash
docker-compose -f docker-compose.dev.yml down
```

#### Вариант 2: Без Docker

**Установка зависимостей:**
```bash
cd backend
pip install -r requirements.txt
pip install -r requirements_integrations.txt
```

**Настройка базы данных:**
1. Убедитесь, что PostgreSQL запущен
2. Создайте базу данных (если еще не создана)
3. Запустите миграции:
   ```bash
   alembic upgrade head
   ```

**Загрузка начальных данных:**
```bash
python initial_data.py
```

**Запуск сервера:**

**Способ 1:** Использовать python main.py (рекомендуется):
```bash
cd backend
python main.py
```

**Способ 2:** Скопировать env.dev в .env и запустить через uvicorn:
```bash
# Из корня проекта
cp env.dev backend/.env
cd backend
uvicorn app.main:app --host 0.0.0.0 --port 8003 --reload
```

**Способ 3:** Использовать переменные окружения напрямую (Linux/Mac):
```bash
cd backend
export $(cat ../env.dev | grep -v '^#' | xargs)
uvicorn app.main:app --host 0.0.0.0 --port 8003 --reload
```

**Примечание:** Config.py настроен на загрузку переменных из файла `.env` в директории backend. Для использования `env.dev` скопируйте его в `backend/.env` или используйте `python main.py`, который автоматически загрузит переменные окружения.

**Проверка работоспособности:**
```bash
curl http://localhost:8003/health
```

Backend должен быть доступен на `http://localhost:8003`

### Шаг 4.4: Установить и запустить Frontend

**Важно:** Frontend находится в отдельном репозитории DBCV_Builder.

#### Клонирование frontend репозитория

```bash
git clone https://github.com/carbonfay/DBCV_Builder.git
cd DBCV_Builder
git checkout dev_build
```

#### Вариант 1: Через Docker (рекомендуется)

**Запуск:**
```bash
docker-compose -f docker-compose.dev.yml up --build
```

После запуска фронтенд будет доступен по адресу: http://localhost:5173

**Остановка:**
```bash
docker-compose -f docker-compose.dev.yml down
```

#### Вариант 2: Без Docker

**Установка зависимостей:**
```bash
npm install
```

или

```bash
yarn install
```

**Запуск:**
```bash
npm run dev
```

или

```bash
yarn dev
```

После запуска фронтенд будет доступен по адресу: http://localhost:5173 (или http://localhost:3000 в зависимости от конфигурации)

**Проверка работоспособности:**
- Открыть браузер: http://localhost:5173
- Должна открыться страница входа/главная страница

**Чеклист:**
- [ ] Репозиторий клонирован
- [ ] Файл env.dev создан и настроен
- [ ] Backend запущен и доступен на http://localhost:8003
- [ ] Frontend запущен и доступен на http://localhost:5173
- [ ] Оба сервиса работают без ошибок

---

## Шаг 5: Реализовать задачу и протестировать

### Шаг 5.1: Создать ветку в Git

**Что такое ветка?** Это копия проекта, где вы будете работать над своей задачей.

#### Обновить main ветку

```bash
# Переключиться на main
git checkout main

# Получить последние изменения
git pull origin main
```

#### Создать ветку для задачи

```bash
# Для интеграций: integration/{service}_{action}
# Примеры:
#   integration/telegram_get_updates
#   integration/openai_chat_completion
#   integration/stripe_create_payment

# Для пресетов: preset/{preset_name}
# Примеры:
#   preset/switch
#   preset/delay
#   preset/loop

git checkout -b integration/telegram_get_updates
# или
git checkout -b preset/switch
```

#### Создать структуру папок (если нужно)

```bash
# Если папка для сервиса еще не существует
mkdir -p backend/app/integrations/telegram
```

### Шаг 5.2: Использовать AI для написания кода

**Это самый важный шаг!** Здесь вы будете использовать AI-инструменты для создания кода.

**Что делать:**

1. **Открыть файл с промптами:** [STUDENT_AI_PROMPT.md](./STUDENT_AI_PROMPT.md)
2. **Выбрать подходящий промпт** для вашей интеграции (или адаптировать базовый шаблон)
3. **Открыть AI-инструмент:**
   - **Cursor:** Нажать `Ctrl+K` (Windows/Linux) или `Cmd+K` (Mac)
   - **Codex (ChatGPT для VS Code):** Нажать `Ctrl+Shift+P` и выбрать команду Codex
   - **ChatGPT:** Открыть https://chat.openai.com или использовать CLI: `chatgpt "ваш запрос"`
   - **Claude:** Открыть https://claude.ai или использовать CLI: `claude "ваш запрос"`
   - **Любой другой** AI-инструмент
4. **Вставить промпт** в AI-инструмент
5. **Получить код** от AI
6. **Скопировать код** и вставить в файл интеграции

**Важно:**
- ✅ AI может ошибаться - всегда проверяйте код!
- ✅ Если код не работает - попросите AI исправить ошибки
- ✅ Не бойтесь задавать уточняющие вопросы AI

### Шаг 5.3: Создать файл интеграции

```bash
# Создать файл
touch backend/app/integrations/telegram/get_updates.py

# Или использовать редактор
code backend/app/integrations/telegram/get_updates.py
```

### Шаг 5.4: Проверить код

После получения кода от AI:

1. **Проверить структуру:**
   - Класс наследуется от `BaseIntegration`
   - Метод `execute()` реализован
   - Метаданные заполнены

2. **Проверить библиотеку:**
   - Используется правильная библиотека из `SAFE_LIBRARIES.md`
   - Версия библиотеки указана корректно

3. **Проверить credentials:**
   - Получение через `credentials_resolver.get_default_for()`
   - Правильный provider и strategy

4. **Проверить обработку ошибок:**
   - Try/except блоки присутствуют
   - Ошибки логируются
   - Возвращается правильный формат ошибки

### Шаг 5.5: Зарегистрировать интеграцию

#### Создать/обновить __init__.py для сервиса

```python
# backend/app/integrations/telegram/__init__.py
from .get_updates import TelegramGetUpdatesIntegration
from app.integrations.registry import registry

# Регистрация интеграции
registry.register(TelegramGetUpdatesIntegration())
```

#### Добавить импорт в главный __init__.py (если нужно)

```python
# backend/app/integrations/__init__.py
try:
    from app.integrations.telegram import *  # noqa: F401, F403
except ImportError:
    pass
```

### Шаг 5.6: Проверить синтаксис

```bash
# Проверить синтаксис Python
python -m py_compile backend/app/integrations/telegram/get_updates.py

# Если ошибок нет - команда завершится без вывода
```

### Шаг 5.7: Тестирование

#### Проверка регистрации

```bash
# Запустить Python интерпретатор
python

# Проверить регистрацию
from app.integrations import registry
integration = registry.get("telegram_get_updates")
print(integration.metadata.name)  # Должно вывести название интеграции
```

#### Проверка через API

1. Запустить backend сервер (если еще не запущен)
2. Открыть `http://localhost:8003/docs`
3. Найти endpoint `/api/integrations/catalog`
4. Выполнить запрос
5. Убедиться, что ваша интеграция в списке

#### Визуальная проверка на фронте

**Важно**: Интеграция должна корректно отображаться в интерфейсе!

1. **Открыть каталог интеграций:**
   - Запустить frontend (если еще не запущен): `npm run dev` (или `yarn dev`)
   - Открыть браузер: `http://localhost:5173`
   - Перейти в конструктор бота
   - Открыть панель интеграций (кнопка с иконкой интеграций)

2. **Проверить отображение интеграции:**
   - [ ] Интеграция видна в списке каталога
   - [ ] Иконка отображается корректно (или placeholder, если иконка еще не загружена)
   - [ ] Название и описание отображаются правильно
   - [ ] Цвет категории отображается (если есть)

3. **Проверить форму конфигурации:**
   - [ ] Нажать на интеграцию в каталоге
   - [ ] Открывается модальное окно с формой конфигурации
   - [ ] Все поля из `config_schema` отображаются
   - [ ] Обязательные поля помечены как обязательные
   - [ ] Типы полей правильные (string, number, integer, boolean, array, object)
   - [ ] Для object и array полей есть JSON редактор
   - [ ] Примеры использования отображаются (если есть)
   - [ ] Валидация форм работает корректно

4. **Проверить вставку интеграции в бот:**
   - [ ] Выбрать интеграцию из каталога
   - [ ] Заполнить форму конфигурации
   - [ ] Нажать "Создать шаг" / "Добавить"
   - [ ] Шаг успешно создан в конструкторе
   - [ ] Шаг отображается с правильной иконкой
   - [ ] При клике на шаг открывается панель редактирования
   - [ ] Конфигурация сохраняется и отображается при повторном открытии

#### Функциональное тестирование

1. **Создать credential:**
   - Открыть интерфейс или использовать API
   - Создать credential для вашего сервиса:
     - Provider: название сервиса (например, "telegram")
     - Strategy: "api_key" или "oauth"
     - Payload: необходимые данные (например, `{"bot_token": "..."}`)

2. **Создать Connection Group с интеграцией:**
   - Создать бота (если еще нет)
   - Создать Connection Group:
     - `search_type`: "integration"
     - `integration_id`: ID вашей интеграции (например, "telegram_get_updates")
     - `integration_config`: конфигурация интеграции

3. **Протестировать выполнение:**
   - Запустить бота
   - Проверить выполнение интеграции
   - Убедиться, что результат правильный
   - Проверить логи на наличие ошибок

#### Написать unit-тесты (опционально, но желательно)

```python
# backend/app/tests/integrations/test_telegram_get_updates.py
import pytest
from app.integrations.telegram.get_updates import TelegramGetUpdatesIntegration

@pytest.mark.asyncio
async def test_telegram_get_updates():
    integration = TelegramGetUpdatesIntegration()
    # Тестовая логика
    pass
```

Запустить тесты:

```bash
pytest backend/app/tests/integrations/test_telegram_get_updates.py
```

### Шаг 5.8: Закоммитить изменения

```bash
# Добавить файлы
git add backend/app/integrations/telegram/get_updates.py
git add backend/app/integrations/telegram/__init__.py

# Сделать коммит
git commit -m "feat(integration): add telegram get_updates integration"

# Push в удаленный репозиторий
git push origin integration/telegram_get_updates
```

**Чеклист:**
- [ ] Ветка создана
- [ ] Код написан с помощью AI
- [ ] Код проверен на ошибки
- [ ] Интеграция зарегистрирована
- [ ] Синтаксис проверен
- [ ] Интеграция видна в API каталоге
- [ ] Интеграция отображается на фронте
- [ ] Форма конфигурации работает
- [ ] Функциональное тестирование пройдено
- [ ] Изменения закоммичены и запушены

---

## Шаг 6: Записать видео

### Требования к видео

1. **Длительность**: 3-5 минут
2. **Формат**: MP4
3. **Разрешение**: минимум 1280x720 (HD)
4. **Качество звука**: четкая речь, без фонового шума

### Что показать в видео

#### Часть 1: Код интеграции (1-2 минуты)

1. Открыть файл `backend/app/integrations/{service}/{action}.py`
2. Показать структуру класса:
   - Класс и наследование от `BaseIntegration`
   - Метод `metadata`
   - Метод `execute`
3. Объяснить ключевые моменты:
   - Как получаются credentials
   - Как используется библиотека
   - Как обрабатываются ошибки

#### Часть 2: Тестирование (1-2 минуты)

1. Показать создание credential:
   - Открыть интерфейс или API
   - Создать credential с нужными данными
2. Показать использование интеграции:
   - Создать Connection Group
   - Настроить integration_config
   - Запустить выполнение
3. Показать результат:
   - Успешное выполнение
   - Данные, которые вернула интеграция

#### Часть 3: Результат (30 секунд)

1. Показать интеграцию в каталоге (если доступно)
2. Показать успешное выполнение еще раз
3. Кратко резюмировать, что было сделано

### Инструменты для записи

- **Windows**: OBS Studio, Windows Game Bar (Win+G)
- **macOS**: QuickTime Player, OBS Studio
- **Linux**: OBS Studio, SimpleScreenRecorder

### Где загрузить видео

1. **YouTube** (рекомендуется):
   - Создать канал или использовать существующий
   - Загрузить видео как "Unlisted" (не в списке)
   - Скопировать ссылку

2. **Google Drive**:
   - Загрузить видео
   - Настроить доступ "Все, у кого есть ссылка"
   - Скопировать ссылку

3. **Другие варианты**:
   - Vimeo
   - Dropbox
   - Любой другой хостинг с публичным доступом

**Чеклист:**
- [ ] Видео записано (3-5 минут)
- [ ] Показан код интеграции
- [ ] Показано тестирование
- [ ] Показан результат
- [ ] Видео загружено и ссылка получена

---

## Шаг 7: Создать Pull Request

### Шаг 7.1: Убедиться, что все готово

Проверить чеклист:

- [ ] Код компилируется без ошибок
- [ ] Интеграция зарегистрирована
- [ ] Протестирована через API
- [ ] Протестирована в боте
- [ ] Визуально проверена на фронте
- [ ] Видео записано и загружено
- [ ] Все изменения закоммичены и запушены

### Шаг 7.2: Создать Pull Request

1. Перейти в GitHub/GitLab
2. Найти свою ветку
3. Нажать "Create Merge Request" / "New Pull Request"

### Шаг 7.3: Заполнить описание PR

Используйте следующий шаблон:

```markdown
## Описание
Реализована интеграция {Service} {Action}

## Изменения
- Добавлен файл `backend/app/integrations/{service}/{action}.py`
- Зарегистрирована интеграция в `backend/app/integrations/{service}/__init__.py`
- Добавлены тесты (если есть): `backend/app/tests/integrations/test_{service}_{action}.py`

## Тестирование
- [x] Проверен синтаксис Python
- [x] Протестирована через API (`/api/integrations/catalog`)
- [x] Протестирована в боте (создан Connection Group, выполнена интеграция)
- [x] Проверена визуально на фронте
- [x] Проверена документация

## Видео
[Ссылка на видео с демонстрацией]

## Чеклист
- [x] Код следует стилю проекта
- [x] Используется правильная библиотека из SAFE_LIBRARIES.md
- [x] Credentials получаются через CredentialsResolver
- [x] Обработка ошибок реализована
- [x] Метаданные заполнены полностью (id, name, description, category, icon_s3_key, config_schema)
- [x] Добавлены примеры использования в examples
- [x] Версия указана корректно ("1.0.0")
```

### Шаг 7.4: Указать reviewers

1. Указать ментора/менеджера как reviewer
2. Добавить соответствующие labels (если есть)

### Шаг 7.5: Создать PR

Нажать "Create Merge Request" / "Create Pull Request"

### Шаг 7.6: Code Review

#### Что ожидать

После создания PR:

1. **Автоматические проверки** (если настроены):
   - Линтеры
   - Тесты
   - Проверка синтаксиса

2. **Ручной review от ментора**:
   - Проверка кода
   - Проверка соответствия требованиям
   - Возможные замечания

#### Как отвечать на замечания

1. **Прочитать замечания внимательно**
2. **Задать вопросы**, если что-то непонятно
3. **Исправить код** согласно замечаниям
4. **Закоммитить исправления**:

```bash
# Внести изменения
git add backend/app/integrations/telegram/get_updates.py
git commit -m "fix(integration): address review comments"
git push origin integration/telegram_get_updates
```

5. **Ответить на замечания** в PR (если нужно)

#### После одобрения

1. Ментор одобрит PR
2. PR будет влит (merged) в main
3. Ваша интеграция станет частью проекта!

**Чеклист:**
- [ ] Все готово к созданию PR
- [ ] PR создан с полным описанием
- [ ] Ссылка на видео добавлена
- [ ] Reviewers указаны
- [ ] Готов отвечать на замечания

---

## Дополнительные разделы

### FAQ

#### Общие вопросы

**Q: С чего начать?**
A: Начните с этого руководства, затем выберите задачу из [TASK_DISTRIBUTION.md](./TASK_DISTRIBUTION.md).

**Q: Какую интеграцию выбрать?**
A: Рекомендуется начать с простых (Telegram, Discord) - они имеют низкую сложность и хорошие примеры.

**Q: Можно ли взять несколько интеграций?**
A: Да, особенно если они из одной категории (например, несколько Telegram интеграций).

**Q: Что делать, если не получается?**
A: 
1. Проверьте примеры существующих интеграций
2. Изучите документацию
3. Обратитесь к ментору
4. Задайте вопрос в общем чате

#### Технические вопросы

**Q: Какую библиотеку использовать?**
A: Проверьте [SAFE_LIBRARIES.md](./backend/app/integrations/SAFE_LIBRARIES.md) - там указаны рекомендуемые библиотеки для каждого сервиса.

**Q: Что делать, если библиотеки нет в requirements.txt?**
A: Добавьте её в `requirements_integrations.txt` и обсудите с ментором.

**Q: Как тестировать интеграцию без реальных credentials?**
A: Используйте моки (unittest.mock) для библиотек и CredentialsResolver.

**Q: Что делать с ошибками компиляции?**
A: 
1. Проверьте синтаксис: `python -m py_compile filename.py`
2. Проверьте импорты
3. Убедитесь, что все зависимости установлены

#### Процесс работы

**Q: Как создать ветку?**
A: См. [Шаг 5.1](#шаг-51-создать-ветку-в-git)

**Q: Что должно быть в Pull Request?**
A: См. [Шаг 7.3](#шаг-73-заполнить-описание-pr)

**Q: Как записать видео?**
A: См. [Шаг 6](#шаг-6-записать-видео)

**Q: Что делать, если есть замечания в code review?**
A: Исправьте код согласно замечаниям, закоммитьте изменения и ответьте на замечания в PR.

### Полезные команды Git

#### Просмотр изменений

```bash
# Показать изменения в файлах
git status

# Показать diff
git diff

# Показать историю коммитов
git log
```

#### Отмена изменений

```bash
# Отменить изменения в файле (до добавления в staging)
git checkout -- filename

# Убрать файл из staging
git reset HEAD filename

# Отменить последний коммит (сохранив изменения)
git reset --soft HEAD~1
```

#### Работа с ветками

```bash
# Показать все ветки
git branch -a

# Удалить локальную ветку
git branch -d branch_name

# Удалить удаленную ветку
git push origin --delete branch_name
```

#### Синхронизация с main

Если в main появились новые изменения:

```bash
# Переключиться на main
git checkout main
git pull origin main

# Вернуться в свою ветку
git checkout integration/telegram_get_updates

# Влить изменения из main
git merge main

# Или использовать rebase (если предпочитаете)
git rebase main
```

### Частые проблемы и решения

#### Проблема: Конфликты при merge

**Решение:**
```bash
# Влить изменения из main
git checkout main
git pull origin main
git checkout integration/your_branch
git merge main

# Разрешить конфликты вручную
# Затем:
git add .
git commit -m "resolve merge conflicts"
```

#### Проблема: Забыл что-то добавить в коммит

**Решение:**
```bash
# Добавить файлы
git add forgotten_file.py

# Добавить к последнему коммиту
git commit --amend --no-edit

# Если уже запушено, нужно force push (осторожно!)
git push origin integration/your_branch --force
```

#### Проблема: Хочу изменить последний коммит

**Решение:**
```bash
# Внести изменения
# Затем:
git add .
git commit --amend -m "новое сообщение коммита"
git push origin integration/your_branch --force
```

### Полезные ресурсы

#### Документация проекта

- **[Документация GitVerse Wiki](https://gitverse.ru/carbonfay/DBCV/wiki/WIKIDBCV1-12)** — полная документация проекта на GitVerse
- [INSTALLATION.md](./INSTALLATION.md) - подробная установка окружения
- [STUDENT_AI_PROMPT.md](./STUDENT_AI_PROMPT.md) - промпты для AI
- [TASK_DISTRIBUTION.md](./TASK_DISTRIBUTION.md) - список задач

#### Документация кода

- `backend/app/integrations/README.md` - руководство по интеграциям
- `backend/app/integrations/SAFE_LIBRARIES.md` - безопасные библиотеки
- `backend/app/integrations/telegram/send_message.py` - пример интеграции
- `backend/app/integrations/base.py` - базовый класс

#### Внешние ресурсы

- [python-telegram-bot docs](https://docs.python-telegram-bot.org/)
- [discord.py docs](https://discordpy.readthedocs.io/)
- [OpenAI Python SDK](https://github.com/openai/openai-python)
- [FastAPI docs](https://fastapi.tiangolo.com/)
- [Git документация](https://git-scm.com/doc)

### Критерии успешного выполнения

#### Обязательные требования

- [ ] Код компилируется без ошибок
- [ ] Интеграция зарегистрирована в registry
- [ ] Credentials получаются через CredentialsResolver
- [ ] Обработка ошибок реализована
- [ ] Метаданные заполнены полностью
- [ ] Добавлены примеры использования
- [ ] Видео записано и загружено
- [ ] Pull Request создан с описанием
- [ ] Интеграция визуально проверена на фронте

#### Дополнительные требования (желательно)

- [ ] Написаны unit-тесты
- [ ] Добавлена иконка в S3
- [ ] Документация обновлена

### Мотивация и возможности

Каждая выполненная задача:

- ✅ **Видна в каталоге** интеграций
- ✅ **Используется** другими студентами и пользователями
- ✅ **Вносит вклад** в развитие платформы
- ✅ **Дает опыт** работы с реальными библиотеками
- ✅ **Улучшает портфолио** студента

#### Возможности после успешного прохождения

При успешном выполнении нескольких задач и демонстрации качественной работы:

- 🎯 **Предложение оффера** - лучшим студентам будет предложена возможность присоединиться к команде на постоянной основе
- 💼 **Рекомендации** - положительные рекомендации для будущих работодателей
- 🏆 **Признание** - ваш вклад будет отмечен в проекте и документации
- 📈 **Карьерный рост** - опыт работы над реальным проектом в вашем портфолио

**Критерии для получения оффера:**
- Качественное выполнение нескольких задач (минимум 3-5 интеграций/пресетов)
- Соблюдение всех требований и стандартов кода
- Активное участие в code review и улучшении проекта
- Позитивная обратная связь от менторов

### Контакты и поддержка

#### Где получить помощь

1. **Документация проекта** - начните отсюда
2. **Примеры кода** - изучите существующие интеграции
3. **Ментор команды** - для сложных вопросов
4. **Общий чат проекта** - для общих вопросов

#### Что делать при проблемах

1. Проверить документацию
2. Изучить примеры
3. Поискать в issues/обсуждениях
4. Обратиться к ментору
5. Задать вопрос в чате

---

## Терминология

### Интеграция

**Интеграция** - это готовый "блок" для работы с внешними сервисами в платформе DBCV. Интеграция позволяет боту взаимодействовать с различными API и сервисами (Telegram, OpenAI, Google Sheets и др.) без написания сложного кода.

**Примеры:** Telegram Send Message, OpenAI Chat Completion, Google Sheets Read

### Пресет

**Пресет** - это готовый "шаблон" для типовых задач в конструкторе ботов. Пресеты упрощают создание сложных логических конструкций.

**Примеры:** IF/ELSE пресет (условное ветвление), Loop пресет (циклы), Delay пресет (задержки)

### Connection Group

**Connection Group** (Группа связей) - это элемент конструктора ботов, который определяет, как бот обрабатывает входящие сообщения или события. Connection Group может быть типа:
- `message` - обработка текстовых сообщений
- `code` - выполнение Python кода
- `integration` - выполнение интеграции с внешним сервисом

### Credentials

**Credentials** (Учетные данные) - это конфиденциальная информация, необходимая для работы с внешними сервисами. Credentials хранятся безопасно и используются интеграциями через CredentialsResolver.

**Примеры:** API ключи, токены доступа, пароли

### Provider и Strategy

**Provider** - это название сервиса, для которого используются credentials (например, "telegram", "openai", "google").

**Strategy** - это способ аутентификации:
- `api_key` - использование API ключа или токена
- `oauth` - использование OAuth авторизации
- `none` - credentials не требуются

### Pull Request / Merge Request

**Pull Request (PR)** или **Merge Request (MR)** - это запрос на добавление вашего кода в основной проект. После создания PR ментор проверяет код и, если все хорошо, вливает его в основную ветку.

### Git ветка

**Ветка (branch)** - это копия проекта, где вы работаете над своей задачей. Каждая задача должна выполняться в отдельной ветке, чтобы не мешать другим разработчикам.

**Формат названия ветки:**
- Для интеграций: `integration/{service}_{action}` (например, `integration/telegram_get_updates`)
- Для пресетов: `preset/{preset_name}` (например, `preset/switch`)

### Registry

**Registry** (Реестр) - это система регистрации всех доступных интеграций в проекте. Каждая интеграция должна быть зарегистрирована в registry, чтобы быть доступной в каталоге.

### Config Schema

**Config Schema** - это JSON Schema, которая описывает параметры конфигурации интеграции. Schema определяет, какие поля нужны для настройки интеграции, их типы, обязательность и значения по умолчанию.

**Поддерживаемые типы полей:**
- `string` - текстовое поле
- `number` - числовое поле (дробные числа)
- `integer` - целочисленное поле
- `boolean` - чекбокс
- `array` - массив (JSON редактор)
- `object` - объект (JSON редактор)
- `enum` - выпадающий список (для string)

### Bot

**Bot** (Бот) - это созданное в платформе DBCV приложение, которое может обрабатывать сообщения, выполнять интеграции и взаимодействовать с пользователями через различные каналы (Telegram, Discord и др.).

### Step

**Step** (Шаг) - это элемент конструктора ботов, который представляет одну операцию или действие. Шаги соединяются между собой через Connection Groups, образуя логику работы бота.

### Channel

**Channel** (Канал) - это способ взаимодействия бота с пользователями. Примеры каналов: Telegram, Discord, VK и др.

### Session

**Session** (Сессия) - это контекст взаимодействия пользователя с ботом. В сессии хранятся переменные пользователя, история сообщений и другая информация о текущем взаимодействии.

### Variables

**Variables** (Переменные) - это данные, которые могут использоваться в боте. Переменные могут быть:
- `bot.*` - переменные бота
- `session.*` - переменные сессии
- `user.*` - переменные пользователя
- `channel.*` - переменные канала

**Пример использования:** `{$user.telegram_chat_id$}` - получить ID чата пользователя в Telegram

### BaseIntegration

**BaseIntegration** - это базовый класс, от которого наследуются все интеграции. Он определяет интерфейс интеграции и методы, которые должны быть реализованы:
- `metadata` - метаданные интеграции
- `execute()` - выполнение интеграции

### CredentialsResolver

**CredentialsResolver** - это сервис, который безопасно получает credentials для интеграций. Интеграции должны использовать CredentialsResolver вместо прямого доступа к credentials.

### Docker и Docker Compose

**Docker** - это платформа для запуска приложений в изолированных контейнерах. **Docker Compose** - это инструмент для управления несколькими контейнерами одновременно.

### API Endpoint

**API Endpoint** - это URL адрес, по которому можно обратиться к API сервера. Например, `/api/integrations/catalog` - endpoint для получения каталога интеграций.

### Swagger / OpenAPI

**Swagger** или **OpenAPI** - это документация API, доступная по адресу `http://localhost:8003/docs`. Здесь можно увидеть все доступные endpoints и протестировать их.

---

**Удачи в реализации! 🚀**
